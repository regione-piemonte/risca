/**
 * Enum che identifica i valori utilizzate per il cmoponente.
 */
export enum RicercaMorositaEnums {
  // Valore utilizzato per convertire in query param il limite valore per accertamento <=
  limiteAccertamentoMinEq = '-',
  // Valore utilizzato per convertire in query param il limite valore per accertamento >
  limiteAccertamentoMag = '+',
}
