/** Costante che compatibile con NgStyle che definisca le proprietà di stile per effettuare l'overflowHidden e l'ellipsis. */
export const lableStyleOverflow = {
  // 'white-space': 'nowrap',
  overflow: 'hidden',
  'text-overflow': 'ellipsis',
};

/**
 * Costante che identifica il nome dell'header utilizzato per non utilizzare il sistema di spinner automatico per una chiamata ai servizi.
 */
export const RISCA_NO_SPINNER_HEADER = 'RISCA_NO_SPINNER_HEADER';

/**
 * Costante che identifica l'estesione dei file di report.
 */
export const REPORT_FILE_EXT: string = '.ods';